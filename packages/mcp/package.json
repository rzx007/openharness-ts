{
  "name": "@openharness/mcp",
  "version": "0.1.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "import": "./src/index.ts",
      "types": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "exit 0",
    "check-types": "tsc --noEmit",
    "test": "vitest run && vitest run --config ../../vitest.e2e.config.ts e2e/docker-stdio.e2e.test.ts",
    "e2e:docker": "vitest run --config ../../vitest.e2e.config.ts e2e/docker-stdio.e2e.test.ts",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "catalog:",
    "@openharness/core": "workspace:*",
    "@openharness/sandbox": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "^5.7.3",
    "vitest": "catalog:"
  }
}
